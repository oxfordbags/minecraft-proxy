# Copy to .env and set the backend address the proxy forwards to.
# Typically the backend server's stable Tailscale IP (`tailscale ip -4` on it).
# e.g. home-server = 100.64.0.1
BACKEND_IP=100.64.0.1
